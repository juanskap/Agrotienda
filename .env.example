MAIL_MODE=log
MAIL_FROM=no-reply@agrotienda.local
MAIL_FROM_NAME=Agrotienda

# Para pruebas, MAIL_MODE=log guarda tickets en storage/mail.
# Para usar mail() de PHP, cambia MAIL_MODE=mail y configura SMTP en XAMPP/PHP.
